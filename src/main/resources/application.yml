server:
  port: 8000
  servlet:
    encoding:
      charset: UTF-8
      force: true
      force-response: true

logging:
  level:
    root: INFO
  file:
    name: /app/log/gateway.log

spring:
  application:
    name: gateway-service
  jwt:
    secret: ${JWT_SECRET_KEY}
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: test-service
          uri: lb://test-service
          predicates:
            - Path=/api/v1/test/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka:8761/eureka

springdoc:
  swagger-ui:
    urls:
      - name: 테스트 서비스
        url: https://nansan.site/api/v1/test/v3/api-docs
    use-root-path: true
